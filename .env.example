PORT=0000 # The port for runing the api
JWT_SECRET_KEY=lhifzbefizebfzfbemrieerjbneob # The JWT key for authentication
CONNECTION_STRING=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${URL + PORT}/${POSTGRES_DB}?sslmode=disable&TimeZone=UTC # Postgres Database Connection String

# Used in docker compose, replaces CONNECTION_STRING
POSTGRES_USER=${POSTGRES_USER}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
POSTGRES_DB=${POSTGRES_DB}