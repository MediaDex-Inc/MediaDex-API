info:
  name: Get Media By Field
  type: http
  seq: 6

http:
  method: GET
  url: "{{basePath}}/fields/:id/media"
  params:
    - name: id
      value: "1"
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
