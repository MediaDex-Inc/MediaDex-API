info:
  name: Create Field
  type: http
  seq: 1

http:
  method: POST
  url: "{{basePath}}/fields"
  body:
    type: json
    data: |-
      {
        "name": "My Field"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
