info:
  name: Update Field
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{basePath}}/fields/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Updated Field"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
