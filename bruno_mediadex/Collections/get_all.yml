info:
  name: Get All Collections
  type: http
  seq: 2

http:
  method: GET
  url: "{{basePath}}/collections"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
