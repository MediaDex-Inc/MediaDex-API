info:
  name: Update Collection
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{basePath}}/collections/:id"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Updated Collection",
        "filters": "sci-fi"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
