info:
  name: Create Collection
  type: http
  seq: 1

http:
  method: POST
  url: "{{basePath}}/collections"
  body:
    type: json
    data: |-
      {
        "name": "My Collection",
        "filters": "action,adventure"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
