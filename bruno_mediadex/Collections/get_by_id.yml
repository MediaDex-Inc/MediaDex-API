info:
  name: Get Collection By Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{basePath}}/collections/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth:
    mode: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
