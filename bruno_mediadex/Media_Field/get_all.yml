info:
  name: Get All Media Fields
  type: http
  seq: 2

http:
  method: GET
  url: "{{basePath}}/mediaFields"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
