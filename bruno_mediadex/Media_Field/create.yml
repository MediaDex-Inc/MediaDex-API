info:
  name: Create Media Field
  type: http
  seq: 1

http:
  method: POST
  url: "{{basePath}}/mediaFields"
  body:
    type: json
    data: |-
      {
        "field_id": 1,
        "media_id": 1,
        "value": "Some value"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
