info:
  name: Get Tags By Media
  type: http
  seq: 6

http:
  method: GET
  url: "{{basePath}}/media/:id/tags"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
