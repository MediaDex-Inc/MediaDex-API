info:
  name: Get Media By Tag
  type: http
  seq: 6

http:
  method: GET
  url: "{{basePath}}/tags/:id/media"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
