info:
  name: Create Tag
  type: http
  seq: 1

http:
  method: POST
  url: "{{basePath}}/tags"
  body:
    type: json
    data: |-
      {
        "name": "Action",
        "color": "#FF5733"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
