info:
  name: Register
  type: http
  seq: 2

http:
  method: POST
  url: "{{basePath}}/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "jgaudin@gmail.com",
        "password": "jgaudin",
        "username": "jgaudin"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
