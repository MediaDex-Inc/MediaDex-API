info:
  name: /register
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/register
  body:
    type: json
    data: |-
      {
        "email": "jgaudin@email",
        "password": "jgaudin",
        "username": "jgaudin"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
