info:
  name: /login
  type: http
  seq: 1

http:
  method: POST
  url: "{{basePath}}/auth/login/:id"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "email": "jgaudin@gmail",
        "password": "jgaudin",
        "username": "jgaudin"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
