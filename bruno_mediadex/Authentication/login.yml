info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{basePath}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "jgaudin@gmail.com",
        "password": "jgaudin",
        "username": "jgaudin"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
